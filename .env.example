# 외부 API 키
#
# 주의: 이 값들을 클라이언트 번들에 그대로 넣으면 사용자가 꺼내 쓸 수 있다.
# 실서비스에서는 자체 서버에 프록시를 두고 src/config.ts의 baseUrl을 그쪽으로 돌린다.
#
# 도보 경로: TMAP 보행자 경로안내 (네이버는 도보 경로 API를 제공하지 않는다)
TMAP_APP_KEY=
#
# 주소 → 좌표: 네이버 클라우드 플랫폼 Maps 지오코딩
NAVER_MAPS_KEY_ID=
NAVER_MAPS_KEY=
