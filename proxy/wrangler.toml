name = "some-time-left-proxy"
main = "src/worker.ts"
compatibility_date = "2026-08-01"

# 비밀값은 여기 두지 않는다. wrangler secret put 으로 넣는다.
#   npx wrangler secret put SEOUL_OPEN_DATA_KEY
#   npx wrangler secret put PROXY_TOKEN
